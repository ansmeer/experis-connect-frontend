export type TUser = {
  id: string;
  name: string;
  picture: string;
  status: string;
  bio: string;
  fun_fact: string;
};

export type TUserPut = {
  name: string;
  picture: string;
  status: string;
  bio: string;
  fun_fact: string;
};
